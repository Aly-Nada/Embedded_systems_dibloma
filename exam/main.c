#include <stdio.h>
#include <stdlib.h>
#include "exam.h"


int main()
{/*
        int  n, number[100];
        printf("Enter the value of N \n");
        scanf("%d", &n);

        printf("Enter the numbers \n");
        for (int i = 0; i < n; ++i)
            scanf("%d", &number[i]);

        sand_watch();

        prime_nums();

        int x;
        scanf("%d",&x);
        d_2_b(x);
*/
    return 0;
}
